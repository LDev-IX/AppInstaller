set_name(ExampleAppInstallerProject);
set_description(This is a description for an example app);
set_author(LDev_IX);
set_website(https://ldev-ix.github.io/);
move(some_important_file.txt, copied_file.txt);
move(File.exe, Program.exe);
mkdir(created_folder);
move(subfolder\subfolder_file.txt, created_folder\subfolder_file_copy.txt);
shortcut(Program.exe);